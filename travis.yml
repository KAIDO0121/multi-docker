sudo: required
language: generic

services:
  - docker


before_install:
  - docker build -t multi-docker ./client/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true multi-docker npm run test -- --coverage

after_success:
  - docker build -t multi-docker ./client/multi-client -t ./client
  - docker build -t multi-docker ./nginx/multi-nginx -f ./nginx
  - docker build -t multi-docker ./server/multi-server -f ./server
  - docker build -t multi-docker ./worker/multi-worker -f ./worker
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push stephengrider/multi-client
  - docker push stephengrider/multi-nginx
  - docker push stephengrider/multi-server
  - docker push stephengrider/multi-worker

